import React from 'react';
import styles from './UserStart.module.css';

function UserStart() {
  
        return (
            <div className = {styles.grad}>
               Hello user
            </div>
        );
}

export default UserStart;